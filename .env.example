# Copy to `.env` and fill in your values
# IMPORTANT: Use KEY=VALUE with no spaces around '=' (Docker-compatible)

# ===== Common (Web + Bot) =====
# MongoDB connection URI
# Example:
# MONGODB_URI=mongodb+srv://user:pass@cluster.example.mongodb.net/tele_finance?retryWrites=true&w=majority
MONGODB_URI=

# ===== Web Server (Node) =====
# Optional settings for server.js
# PORT=3000
# DB_NAME=tele_finance
# COLLECTION=expenses
# API_KEY=replace-me   # If set, API requires header: x-api-key: <value>
# FORCE_CHAT_ID=957879521  # Restrict API data to a single chat/user id

# ===== Telegram Bot (Python) =====
# Telegram bot token
TELEGRAM_TOKEN=
# DashScope API key (Qwen)
DASHSCOPE_API_KEY=

# Allowed Telegram user IDs (security). Choose ONE of:
# - Comma or space separated list
# ALLOWED_USER_IDS=123456,789012
# - Or a single ID via FORCE_CHAT_ID (compat)
# FORCE_CHAT_ID=123456
